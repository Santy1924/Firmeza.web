@model IEnumerable<Firmeza.web.Data.Entity.Venta>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/LayoutDashboard.cshtml";
}

<h1>Ventas</h1>

<div class="mb-4">
    <a asp-action="Create" class="btn-create">
        <i class="fa-solid fa-plus"></i> Create New
    </a>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MetodoPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoVenta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MetodoPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoVenta)
            </td>
            <td>
                <div class="d-flex justify-content-start">
                    <a asp-action="Edit" asp-route-id="@item.Id" class="action-link action-edit" title="Edit">
                        <i class="fa-solid fa-pen-to-square"></i>
                    </a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="action-link action-details" title="Details">
                        <i class="fa-solid fa-eye"></i>
                    </a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="action-link action-delete" title="Delete">
                        <i class="fa-solid fa-trash"></i>
                    </a>
                    <a asp-action="GeneratePdf" asp-route-id="@item.Id" class="action-link action-pdf" title="PDF">
                        <i class="fa-solid fa-file-pdf"></i>
                    </a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
